info:
  name: GuiEmail
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/Common/GuiEmail"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  params:
    - name: x-api-key
      value: ""
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
          "body":"test body",
          "title":"title test",
          "to":"hauphvn@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

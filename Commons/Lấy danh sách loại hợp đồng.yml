info:
  name: Lấy danh sách loại hợp đồng
  type: http
  seq: 3

http:
  method: POST
  url: "{{url_old}}/Common/GetListContractType"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

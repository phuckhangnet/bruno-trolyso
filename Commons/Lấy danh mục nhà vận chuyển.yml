info:
  name: Lấy danh mục nhà vận chuyển
  type: http
  seq: 6

http:
  method: POST
  url: "{{url_old}}/Common/GetAllActiveShippingCarriers"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

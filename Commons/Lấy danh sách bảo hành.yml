info:
  name: Lấy danh sách bảo hành
  type: http
  seq: 8

http:
  method: POST
  url: "{{url_old}}/Common/GetProductWarranties"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

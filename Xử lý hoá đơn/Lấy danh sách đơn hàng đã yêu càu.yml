info:
  name: Lấy danh sách đơn hàng đã yêu càu
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/DataManagement/GetRequestedInvoices"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

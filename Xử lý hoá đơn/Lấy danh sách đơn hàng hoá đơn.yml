info:
  name: Lấy danh sách đơn hàng hoá đơn
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/DataManagement/GetInvoicedOrdersByDate"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: |-
      {
          "SearchText":"",
          "FromDate":"2026-01-01",
          "ToDate":"2026-12-30",
          "PageNumber":1,
          "PageSize":10
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

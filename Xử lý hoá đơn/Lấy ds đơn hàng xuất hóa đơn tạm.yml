info:
  name: "Lấy ds đơn hàng xuất hóa đơn tạm "
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/DataManagement/GetOrderListForInvoicing"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: |-
      {
          "SearchText": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Xóa hóa đơn tạm
  type: http
  seq: 17

http:
  method: POST
  url: "{{url}}/DataManagement/CancelTemporaryInvoice"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: |-
      {
        "IdHoaDon": 8
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

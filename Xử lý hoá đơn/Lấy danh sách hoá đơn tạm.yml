info:
  name: Lấy danh sách hoá đơn tạm
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/DataManagement/GetTemporaryInvoicesOfTheDay"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: |-
      {

      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

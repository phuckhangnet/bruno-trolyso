info:
  name: "[NhomHang]-Xoá danh mục nhớm hàng"
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/DanhMucNhomHang/Xoa/1"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

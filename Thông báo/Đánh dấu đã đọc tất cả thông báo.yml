info:
  name: Đánh dấu đã đọc tất cả thông báo
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/ThongBao/NhanVien/DanhDauDaDocTatCa"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: |-
      {
          "PageSize":20,
          "PageNumber":1,
          "Type":"TAC_CA" // TAC_CA | DON_HANG    
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

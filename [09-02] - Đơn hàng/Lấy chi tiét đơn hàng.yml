info:
  name: Lấy chi tiét đơn hàng
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/DonHang/ChiTietDonHang/82"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

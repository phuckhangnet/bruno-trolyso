info:
  name: Tạo danh mục chi phí
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/ChiPhi/DanhMuc/TaoMoi"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "{{userIdLogin}}"
    - name: idCuaHang
      value: "{{idCuaHang}}"
  body:
    type: json
    data: |-
      {
          "Name": "test",
          "Description": "test",
          "Code": "eee"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
